rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Permitir lectura pública de las imágenes de productos
    match /products/{allPaths=**} {
      allow read: if true;
      allow write: if request.auth != null; // O cambia según tus necesidades de seguridad
    }
  }
}
