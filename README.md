### Instalar las dependencias de firebase
npm install firebase
## Firebase CLI-Hosting
npm install -g firebase-tools

## Accede a Google
firebase login

## Inicia el proyecto: Ejecuta el siguiente comando en el directorio raíz de tu app
firebase init
firebase deploy
